{
  "name": "create-ipfs-app-monorepo",
  "version": "1.1.2",
  "description": "Create IPFS App.",
  "private": true,
  "workspaces": [
    "packages/create-ipfs-app",
    "packages/ipfs-scripts"
  ],
  "scripts": {
    "publish:create-ipfs-app": "cd packages/create-ipfs-app && npm publish",
    "publish:ipfs-scripts": "cd packages/ipfs-scripts && npm publish"
  }
}
